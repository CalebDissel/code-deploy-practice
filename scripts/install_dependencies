#!/bin/bash
set -euo pipefail

# Make sure the target path exists even before we copy files
mkdir -p /var/www/html

# Install Apache (httpd) if missing
if ! rpm -q httpd >/dev/null 2>&1; then
  dnf -y install httpd
fi

# Enable and start it
systemctl enable httpd
systemctl restart httpd
